// User Input
int num = int.Parse(Console.ReadLine());

//Check if the number is in between 100 and 200 or equal to 0
if (num >= 100 && num <= 200 || num == 0)
{
    Console.WriteLine();
}
else
{
    Console.WriteLine("invalid");
}