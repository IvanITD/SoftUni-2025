// Creating a for loop that prints all the number for x and y in the range of 1 to 10.
for (int x = 1; x <= 10; x++)
{
    for (int y = 1; y <= 10; y++)
    {
        // Printing the result in the format x * y = result
        Console.WriteLine($"{x} * {y} = {x * y}");
    }
}